<?xml version="1.0" encoding="UTF-8"?>
<project basedir="../../WAS1_SUGI" default="compile" name="COMP">

  <!-- Project Default Directory -->
	<property name="src.dir" location="${basedir}\src"/>
	<property name="lib.dir" location="${basedir}\lib"/>
	<property name="dist.dir" location="${basedir}\dist"/>
	<property name="classes.dir" location="${basedir}\web\WEB-INF\classes"/>
	<property name="properties.dir" location="${basedir}\conf"/>

	<!-- Project Default Classpath -->
	<path id="project.classpath">
		<fileset dir="${lib.dir}">
      		<include name="*.jar"/>
    	</fileset>
		<fileset dir="${lib.dir}\tomcat">
      		<include name="*.jar"/>
    	</fileset>
		<fileset dir="${lib.dir}\spring">
      		<include name="*.jar"/>
    	</fileset>
    	<fileset dir="${lib.dir}\apache">
      		<include name="*.jar"/>
    	</fileset>
  </path>

  <echo message ="#### SET PROJECT CLASSPATH ####"/>

  <!-- Make Directory -->
  <target name="init">
  	<tstamp>
       <format property="time" pattern="yyyy-MM-dd hh:mm" />
    </tstamp>
	<mkdir dir="${classes.dir}"/>
  	<mkdir dir="${basedir}\lib"/>
  </target>
 
  <echo message ="#### COMPILE COMPLETE ####"/>

    <target name="compile" depends="init">
    <javac destdir="${classes.dir}" srcdir="${src.dir}" optimize="true" debug="true" deprecation="true" encoding="utf-8" includeantruntime="false">
      <classpath refid="project.classpath"/>
    </javac>
    <!--
    <copy todir="${classes.dir}">
    	<fileset dir="${properties.dir}" includes="**/*.properties,**/*.xml"/>
    </copy>
    -->
    
    <jar destfile="${lib.dir}\MARU_v1.jar" basedir="${classes.dir}" excludes="com/pgmate/test/**"/>
    <jar destfile="${lib.dir}\MARU_lib.jar" basedir="${classes.dir}" includes="com/pgmate/lib/**,com/pgmate/sugi/util/**"/>
    
  </target>

  <echo message ="#### START COMPILE  ####"/>

</project>
